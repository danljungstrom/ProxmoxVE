# Audit backlog — happier-dev/ProxmoxVE (Happier scripts) — 2026-06-25

Source report: ./2026-06-25-audit.md · GitHub: local-only (not yet ticketed)
Remediation: worked on branch `daemon-auth` (PR2), 2026-06-25. Not yet shipped (no push/PR).

| # | Finding | Dimension | Severity | Effort | Epic | Issue # | Status |
|---|---------|-----------|----------|--------|------|---------|--------|
| 1 | `happier_github_curl` secret-handling duplicated byte-identical | dedup | High | M | extract-common-func | - | done |
| 2 | UI-bundle minisign+sha256 verify duplicated AND drifted | dedup | High | L | extract-common-func | - | done |
| 3 | `from_source` ignores `HAPPIER_PVE_SERVER_PORT` → unreachable | docs/bug | High | M | from_source-correctness | - | done |
| 4 | `channel_*` helper family duplicated | dedup | Medium | M | extract-common-func | - | done |
| 5 | `resolve_*` / `ui_release_tags_for_channel` trio duplicated | dedup | Medium | S | extract-common-func | - | done |
| 6 | CLI resolver duplicated with diverging contracts | dedup | Medium | M | extract-common-func | - | done |
| 7 | minisign override warning fires on different conditions (ct spurious) | consistency | Medium | S | extract-common-func | - | done |
| 8 | json `config_path` only correct for installers mode | docs | Medium | S | from_source-correctness | - | done |
| 9 | No CI shellcheck/shfmt gate on the happier scripts | tests | Medium | S | ci-lint-gate | - | done |
| 10 | Highest-risk bash logic has no automated coverage | tests | Medium | M | ci-lint-gate | - | done |
| 11 | Release-repo default hardcoded in both UI-bundle copies | dedup | Low | S | extract-common-func | - | done |
| 12 | `restart_happier_unit` wrapper duplicated | dedup | Low | S | extract-common-func | - | done |
| 13 | `normalize_happier_channel` CRLF divergence (ct omits `tr -d '\r'`) | consistency | Low | S | extract-common-func | - | done |
| 14 | `channel_ui_current_dir` computed two different ways | consistency | Low | S | extract-common-func | - | done |
| 15 | `.editorconfig max_line_length=120` violated in both files | consistency | Low | M | cosmetic | - | wontfix |
| 16 | `ct/happier.sh` would fail upstream `script_format.yml` header check | tests | Low | S | ci-lint-gate | - | wontfix |

Status values: open · in-progress · pr-open · done · wontfix.

## Resolution notes
- **extract-common-func** (1,2,4,5,6,7,11,12,13,14): created `misc/happier-common.func` — a shared lib fetched via `INSTALLER_REPO`/`INSTALLER_REF` (like build.func) and sourced by both `ct/happier.sh` (after build.func) and `install/happier-install.sh` (after FUNCTIONS_FILE_PATH). All duplicated helpers moved there; scripts shrank ~543 lines. Verification (minisign `-Vm` + `sha256sum -c` + atomic swap) preserved exactly; `happier_github_curl` byte-identical. Codex-reviewed EXTRACTION-SAFE (verify/secret/source-order all PASS). #7/#13/#14 fixed in the single shared copy.
- **from_source-correctness** (3,8): the from_source server binds `HAPPIER_STACK_SERVER_PORT` (traced through `@happier-dev/stack@0.2.0` CLI→resolver→bind) — installer now sets it in `SETUP_ENV` + persists to `$STACK_ENV_FILE` when the override is set; json note scoped (config_path is installer-mode; from_source uses `~/.happier/stacks/main/env`).
- **ci-lint-gate** (9,10,16): added `.github/workflows/happier-lint.yml` — fork-owned (not community-scripts-gated), runs `bash -n` (scripts + patched funcs + happier-common.func), `shellcheck` (honoring `.vscode/.shellcheckrc`), shfmt diff (informational), and json validation on PRs. #16: ct's intentional build.func-bootstrap header divergence is documented in the workflow comment; only relevant if upstreaming → wontfix.
- **#15 (editorconfig)** wontfix: the >120-col lines are color-coded `echo`/whiptail dialog strings; `max_line_length` is advisory (not formatter-enforced) and wrapping these harms readability/risks breaking output. Accepted.
