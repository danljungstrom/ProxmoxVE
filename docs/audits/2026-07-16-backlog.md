# Audit backlog — happier fork of ProxmoxVE (feat/devbox-agents-autoupdate) — 2026-07-16

Source report: ./2026-07-16-audit.md · GitHub: local-only

| # | Finding | Dimension | Severity | Effort | Epic | Issue # | Status |
|---|---------|-----------|----------|--------|------|---------|--------|
| A1 | Unguarded `relay host install` aborts update between CLI self-update and daemon restart | stability | Medium* | S | update-path | - | done |
| A2 | Update reinstalls+restarts relay on autostart-disabled installs (no is-enabled probe) | stability | Medium | S | update-path | - | done |
| A3 | Update relay reinstall drops --env args, --auto-update-at, capability probe | stability | Medium | M | update-path | - | done |
| A4 | ensure_relay_host_installed masks reinstall failure; no post-check of unit | stability | Medium | S | update-path | - | done |
| A5 | --auto-update capability probe SIGPIPE/pipefail false negative silently disables opt-in | stability | Low | S | update-path | - | done |
| A6 | `ln -sfn` symlink swap not atomic (comment claims it is) | stability | Low | S | ui-bundle | - | done |
| A7 | Refresh-mode unguarded move-aside mv can nest staging in live dir, report false success | stability | Low | S | ui-bundle | - | done |
| A8 | Tag-fallback resolver shares one stdout redirect; partial attempt corrupts next JSON | stability | Low | S | ui-bundle | - | done |
| A9 | Generated /usr/bin/update streams curl\|bash, no retry/timeout hardening | stability | Low | S | update-helper | - | done |
| A10 | HSTACK_BIN login-shell fallback capture pollutable → false abort after long build | stability | Low | S | hstack-layout | - | done |
| B1 | Exported PAT + tailscale key inherited by third-party root children (npm lifecycle etc.) | security | Medium | S | secrets | - | done |
| B2 | codex npm install unpinned root-wide, scripts enabled (prior B3 pin never applied) | security | Low | S | secrets | - | done |
| B3 | UI-bundle verify has no rollback/version-binding protection (signed downgrade replays) | security | Low | M | ui-bundle | - | done |
| E1 | Two /usr/bin/update writers; shipped one dropped proxy-env sourcing (drifted) | dedup | Medium | S | update-helper | - | done |
| E2 | json twin files unguarded: CI neither triggers on nor cmp-checks frontend copy | dedup | Medium | S | ci-guard | - | done |
| E3 | Hstack layout default literals duplicated ct vs install | dedup | Low* | S | hstack-layout | - | done |
| E4 | Tailscale manual-enroll guidance ×4; installers copy omits captured AuthURL | dedup | Low | M | tailscale-ux | - | done |
| F1 | Tailscale key prompt: Cancel aborts whole install; PAT prompt Cancel skips (F7 miss) | consistency | Medium | S | tailscale-ux | - | done |
| F6 | CI PATCHED_FUNCS lists core.func which carries no fork patch | consistency | Low | S | ci-guard | - | wontfix |
| D1 | from_source path zero coverage at any layer (no unit tests, no matrix case) | tests | High | L | test-harness-2 | - | open |
| D2 | update_script untested + untestable (`function` keyword defeats extractor); channel detection, timer name, legacy branch uncovered | tests | High | M | test-harness-2 | - | open |
| D3 | extract_function blind to `function name()` style (narrowed; heredoc-depth as hardening) | tests | Medium | S | test-harness-2 | - | done |
| D4 | Runner rc-only check: no-op test file passes silently (no plan/count assertion) | tests | Medium | S | test-harness-2 | - | done |
| D5 | set_env_kv/remove_env_kv untested; escape asymmetry update-vs-append; unescaped keys | tests | Medium | S | test-harness-2 | - | open |
| D6 | enroll_tailscale_node outcome classification untested (drives epilogue flags) | tests | Medium | M | test-harness-2 | - | open |
| D7 | happier_github_curl auth tier + tag-fallback loop never executed by suite | tests | Medium | M | test-harness-2 | - | open |
| D8 | resolve_hstack_layout jq parsing + workspace fallback untested | tests | Medium | M | test-harness-2 | - | open |
| D9 | Nested-bundle extract-root branch dead in tests | tests | Low | S | test-harness-2 | - | open |
| D10 | lib.sh skip() double-reports ok for the same test | tests | Low | S | test-harness-2 | - | done |
| D11 | Missing-minisign branch never runs in CI (mutually-exclusive skips) | tests | Low | S | test-harness-2 | - | open |
| D12 | URL normalizer port-preserve/query-drop behavior untested | tests | Low | S | test-harness-2 | - | done |
| D13 | retry_until + PAT charset gate untested | tests | Low | S | test-harness-2 | - | done |
| G1 | Stable labeled "recommended" while matrix documented broken vendor stable build | docs | Medium** | S | docs-truth-2 | - | done |
| G2 | Spec deviations section missing 07-02/03 deviations (probe, baked pins, reconciliation) | docs | Low | S | docs-truth-2 | - | done |
| G3 | json unattended-install note omits PHS_SILENT=1 | docs | Low | S | docs-truth-2 | - | done |
| M1 | actions/checkout v6 → v7.0.0 | modernity | Low | S | ci-guard | - | done |
| P1 | UI refresh re-downloads full archive when installed version already current | perf | Medium | S | ui-bundle | - | done |
| P2 | Tailscale Serve loop ~4-5 min worst-case stall, no early bail, no progress | perf | Medium | M | tailscale-ux | - | in-progress |
| P3 | Retry helpers sleep after final failed attempt (~45 s amplified in Serve loop) | perf | Low | S | tailscale-ux | - | done |
| P4 | CI re-downloads pinned tools + full apt update every run (no caching) | perf | Low | S | ci-guard | - | in-progress |

Status values: open · in-progress · pr-open · done · wontfix.
Update Issue # after GitHub sync; update Status as audit-remediate progresses.

Notes:
- Codex alignment (codex-cli 0.144.5): 36 agree / 3 modify / 10 drop / 1 needs-decision over 50
  candidates. Dropped items recorded in the report ("Dropped after Codex alignment").
- \* Codex-modified severities applied as default: A1 High→Medium, E3 Medium→Low (pending user
  confirmation, see report "Pending decisions").
- \*\* G1 is needs-decision: default plan = re-verify stable channel on current vendor CLI first;
  warn only if still broken.
- Adversarial verify: no candidate refuted; A3's env-reset impact and A7's trigger conditions
  noted as unproven/rare respectively in the report.
- Cross-dimension merges: E2 (dedup+consistency), A3 (dedup+consistency+stability).
- Suggested remediation order: update-path + D1/D2 (High/Medium correctness with documented
  real-world triggers) → ui-bundle (P1+B3 share a fix) → secrets → ci-guard → tailscale-ux →
  test-harness-2 rest → docs-truth-2 → hstack-layout.

## Remediation progress (2026-07-17)

Local-only, grouped commits on `feat/devbox-agents-autoupdate`. Each epic verified with
`shellcheck 0.11.0 --norc -S info` + `shfmt -i 2 -ci` + `tests/happier/run.sh` before commit.
Findings that change core install/update *behavior* are marked **LXC-gated**: their commits must
not be pushed until an unattended LXC matrix run on pve-hp confirms them.

- **update-path (A1/A2/A4/A5 done):** A1 guarded relay reinstall + warn-continue; A2 capture &
  restore autostart (is-enabled/is-active) around the reinstall; A4 post-check the relay unit and
  hard-fail if still missing; A5 capture `--help` then grep a here-string (no pipefail SIGPIPE
  false-negative). **LXC-gated** smoke recommended before push (touches the live update path).
  **A3 done (LXC-verified 2026-07-17):** confirmed live on a dev-channel devbox CT that the bare
  `relay host install` reinstall DOES reset the relay env — it rewrote `HAPPIER_SERVER_UI_DIR` from
  the managed `/var/lib/happier-dev/ui-web/current` back to the vendor default `/opt/...`, so the
  freshly-refreshed UI bundle would stop being served after every update. Fix: update_script now
  re-passes the install-managed env (`HAPPIER_SERVER_UI_DIR` from `channel_ui_current_dir`, plus
  `HAPPIER_SERVER_HOST`/`PORT`/`HAPPIER_PUBLIC_SERVER_URL`/`HAPPIER_WEBAPP_URL` from server.env).
  End-to-end update re-test: rc=0, UI_DIR restored to `/var/lib/...`, relay active. The live test
  also caught two errexit hazards in the first fix attempt (`[[ -n ]] &&` and `var=$(grep|…)` both
  abort under the update's `set -Eeuo pipefail`) — shellcheck did not flag them; fixed with an `if`
  guard + `|| true`.
- **ui-bundle (A6/A7/A8/B3/P1 done):** A6 atomic symlink swap (temp symlink + `mv -T`); A7 guard
  the refresh move-aside mv + sweep stale `.tmp.*`/`.old.*` debris; A8 buffer each tag attempt in
  its own file so a partial failed fetch no longer corrupts the next tag's JSON; P1 read the
  installed version from the `current` symlink and short-circuit a same-version refresh before any
  download; B3 refuse a downgrade to an older (still validly-signed) bundle via `sort -V`. Added
  3 fixture tests (upgrade-swaps, same-version-skips, downgrade-refused); suite now 10 UI tests.
  Fully local-verifiable (no LXC gate). A8's resolver stays stubbed in tests → real exercise is D7.
- **update-helper (A9 done, E1 part 1 done):** rewrote write_update_helper to (A9) download the
  fork's ct/happier.sh to a temp file with `--retry/--connect-timeout/--max-time` and run it,
  instead of an unbounded `curl | bash` that can execute a truncated script; and (E1 part 1) source
  `/etc/profile.d/90-http-proxy.sh` so `update` keeps proxy env on proxied networks (was dropped vs
  customize()'s writer). Verified by rendering the generated helper + shellcheck. **E1 part 2**
  (revert the now-dead `misc/install.func` update-line override to upstream + drop install.func from
  CI PATCHED_FUNCS + update the upstream-sync memory) is folded into the **ci-guard** epic with F6,
  since it shares the fork-patch-surface/CI concern. `finish_install` always overwrites
  customize()'s `/usr/bin/update`, so the override is confirmed dead in both install methods.
- **secrets (B1/B2 done):** B1 `unset HAPPIER_PVE_GITHUB_PAT HAPPIER_PVE_TAILSCALE_AUTHKEY` after
  they are copied into locals (confirmed no later reads), so the exported secrets are not inherited
  by the third-party root children (claude.ai bootstrap, NodeSource, `npm install -g` lifecycle).
  B2 pin codex to `@openai/codex@0.144.5` (verified current latest on npm 2026-07-17) instead of an
  unpinned root-wide install. Fully local-verifiable.
- **ci-guard (E2/M1 done, E1 part 2 done, F6 wontfix, P4 deferred):** E2 add
  `frontend/public/json/happier.json` to the lint path triggers + a `cmp` step failing if the two
  installer JSONs drift (verified identical today, guard passes). M1 actions/checkout v6→v7. E1
  part 2 revert the dead `misc/install.func` update-line repo override to the exact upstream literal
  (write_update_helper overwrites it; install.func keeps its essential core/error_handler/tools URL
  overrides, so it stays a patched file). **F6 wontfix — FALSE POSITIVE:** `git diff` vs *current*
  community-scripts shows core.func DOES carry a fork patch (the `cleanup_lxc` `timeout`/
  `DPkg::Lock::Timeout` apt hardening); it has no `happier`/`fork` comment marker, so the audit's
  marker-grep missed it. core.func must stay in PATCHED_FUNCS. (Memory [[upstream-sync-plan]]
  refined: detect fork patches via `git diff upstream/main`, not marker-grep.) Verified: YAML parses,
  cmp guard passes, install.func `bash -n` clean. **P4 deferred** (in-progress): the tool-cache
  restructure can't be runtime-verified without a GitHub runner and is the lowest-value item;
  not shipping it unverified.
- **tailscale-ux (F1/E4/P3 done, P2 deferred):** F1 the Tailscale pre-auth-key passwordbox Cancel
  now skips (`|| HAPPIER_PVE_TAILSCALE_AUTHKEY=""`) instead of aborting the whole install, matching
  the PAT prompt; wording updated. E4 the installers-method epilogue now prints the captured
  `TAILSCALE_AUTH_URL` (lazy-resolved) like the from_source epilogue already did. P3 both retry
  loops (`retry_until`, `resolve_tailscale_https_url_with_retries`) skip the sleep after the final
  failed attempt (verified: 3 attempts × 1s → 2s, not 3), using an errexit-safe `if` guard.
  Verified: shellcheck/shfmt clean, suite green. **P2 deferred** (in-progress): the Serve-loop
  early-bail needs the exact no-HTTPS-capability signal from a live tailnet to avoid false bails on
  valid setups, and the periodic-progress display is framework-spinner-sensitive → LXC-gated.
- **test-harness-2 (D3/D4/D10/D12/D13 done):** D3 extract_function now matches the `function name()`
  keyword form (proven: it extracts ct/happier.sh update_script, 152 lines, previously 0 — this
  unblocks D2's testability). D4 run.sh now fails if a test file emits no `# N tests` plan line
  (negative-tested with a no-op file → PLAN FAIL, rc=1). D10 run_tests no longer prints a second
  `ok` line for a skipped test. D12 added port-preserve + query/hash-drop assertions to the URL
  normalizer test. D13 extracted the PAT charset gate into `is_valid_github_pat` (wired at the call
  site) and added retry_until timing tests (no trailing sleep) + PAT-gate tests; suite grew to
  6+5+10+6 tests. Verified: shellcheck 0.11.0/shfmt clean, `run.sh` green.
  **Remaining (open):** D1 (from_source matrix case — **LXC**), D2 (channel-detection unit tests +
  the `-updater.timer` name E2E — extraction now unblocked by D3; the E2E leg is **LXC**),
  D5 set_env_kv/remove_env_kv, D6 enroll_tailscale_node classification, D7 happier_github_curl
  auth/tag-fallback, D8 resolve_hstack_layout, D9 nested-bundle branch, D11 hermetic missing-minisign.
  These need more install-fn test scaffolding; not closed this session.
- **docs-truth-2 (G2/G3 done, G1 open):** G2 added a "Deviations (matrix hardening, 2026-07-02/03)"
  subsection to the design spec covering the `--auto-update` capability probe, the baked
  update-helper pins, and the relay-unit reconciliation reinstall. G3 added `PHS_SILENT=1` to the
  json unattended-install note in BOTH twins (verified byte-identical + contract-valid, so the new
  E2 guard passes). **G1 done (LXC-verified 2026-07-17):** re-ran a fresh stable-channel install on
  the current vendor CLI — STILL broken (CLI + UI install, then `relay host install` fails with
  "relay runtime did not become healthy (http://127.0.0.1:3005/v1/version)", install exit 1),
  matching the 07-03 finding. Per the default plan (warn if still broken): removed the false
  "Stable (recommended)"/"recommended for production" labeling from the channel prompt (now notes
  the known failing build), and added an install-time `msg_warn` for preset/unattended stable
  installs that skip the prompt. Both carry a dated comment to remove once the vendor stable build
  works again.
- **hstack-layout (E3/A10 done):** E3 hoisted the from_source layout default literals
  (`HAPPIER_STACK_DEFAULT_BIN/HOME/LABEL/ENV`) into misc/happier-common.func; ct/happier.sh's legacy
  update branch and the installer's from_source path now reference them — the `.happier-stack` /
  `dev.happier.stack` literals live in one place. Removed the now-dead `STACK_NAME` chain (it was
  assign-only after the env default centralized) and its default. A10 the login-shell HSTACK_BIN
  fallback now pipes through `grep -m1 '^/'` so MOTD/profile stdout noise from `bash -lc` can't
  pollute the capture and trigger a false "hstack not found" abort. Verified: shellcheck/shfmt
  clean, suite green.
