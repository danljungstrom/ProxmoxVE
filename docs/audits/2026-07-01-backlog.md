# Audit backlog — happier fork of ProxmoxVE (feat/devbox-agents-autoupdate) — 2026-07-01

Source report: ./2026-07-01-audit.md · GitHub: local-only

| # | Finding | Dimension | Severity | Effort | Epic | Issue # | Status |
|---|---------|-----------|----------|--------|------|---------|--------|
| A1 | Unguarded setup_nodejs fatal under set -e aborts default devbox install midway | stability | High | S | robustness | - | in-progress |
| A2 | `●` failure-marker breaks daemon-unit discovery in update path (add --plain) | stability | Medium | S | robustness | - | in-progress |
| A3 | `self update` failure swallowed, unconditional msg_ok | stability | Medium | S | robustness | - | in-progress |
| A4 | cli_bin not re-validated after self-update re-resolve | stability | Low | S | robustness | - | in-progress |
| A5 | happier_github_curl no timeout/retry; rate-limit hard-aborts install/update | stability | Medium | S | robustness | - | in-progress |
| A6 | UI-bundle swap deletes live dir before replacing (same-version refresh) | stability | Medium | S | robustness | - | in-progress |
| A7 | Daemon drop-in: reload/restart failures masked; PAT unvalidated before unit write | stability | Medium | S | robustness | - | in-progress |
| A8 | Drop-in write subshell or-true guard masks ENOSPC/short writes | stability | Low | S | robustness | - | in-progress |
| A9 | install_managed_ui_bundle: no cleanup trap; 10 duplicated rm-rf/exit blocks | stability | Low | S | robustness | - | in-progress |
| B1 | Daemon PAT via Environment= readable by any local user via systemctl show → EnvironmentFile | security | Medium | S | hardening | - | in-progress |
| B2 | Tailscale pre-auth key on argv (/proc cmdline, ≤120s) — pre-existing on main | security | Low | S | hardening | - | in-progress |
| B3 | Agent CLIs installed as root via unpinned npm -g; Anthropic recommends native/apt | security | Medium | M | hardening | - | in-progress |
| C1 | happier-common.func excluded from CI shellcheck (fork-owned, passes clean today) | ci | Medium | S | ci-gate | - | in-progress |
| C2 | shfmt: 2 files fail declared style; step can never fail; binary unpinned | ci | Medium | S | ci-gate | - | in-progress |
| C3 | CI shellcheck 0.9.0 via apt vs 0.11.0 current — pin upstream binary | ci | Medium | S | ci-gate | - | in-progress |
| C4 | .shellcheckrc blanket disables (SC2086 etc.) re-applied to fork scripts in CI | ci | Medium | M | ci-gate | - | in-progress |
| C5 | actions/checkout@v4 → v6 | ci | Medium | S | ci-gate | - | in-progress |
| C6 | happier.json CI check parse-only, no contract-field validation | ci | Low | S | ci-gate | - | in-progress |
| D1 | minisign+sha256 verify flow: no functional tests (bats fixture suite) | tests | Medium | M | test-harness | - | in-progress |
| D2 | Channel-mapping helpers untested (table-test once harness exists) | tests | Low | S | test-harness | - | in-progress |
| D3 | URL normalizers + HH:MM untested; python heredoc fails silent to "empty" | tests | Medium | S | test-harness | - | in-progress |
| D4 | 6-case LXC matrix has no execution record | tests | Medium | S | test-harness | - | open |
| E1 | Tailscale enroll+serve duplicated managed vs from_source; drift proven by history | dedup | Medium | M | unification | - | in-progress |
| E2 | URL-precedence head repeated ~4× (latent drift only; behaviorally consistent) | dedup | Low | M | unification | - | wontfix |
| E3 | Daemon-unit discovery + restart idiom duplicated (hygiene; pair with A2) | dedup | Low | S | unification | - | in-progress |
| E4 | hstack `where --json` parsing duplicated ct vs install | dedup | Medium | M | unification | - | in-progress |
| E5 | Channel→stack-package default mapping duplicated; back-compat alias one-sided | dedup | Medium | S | unification | - | in-progress |
| E6 | Post-install epilogue copy-pasted managed vs from_source (3 blocks) | dedup | Medium | M | unification | - | in-progress |
| E7 | Four near-identical retry/poll loops → retry_until helper | dedup | Low | S | unification | - | in-progress |
| E8 | fetch-raw-github bootstrap block ×3 (chicken-and-egg limits extraction) | dedup | Low | S | unification | - | in-progress |
| F1 | INSTALLER_REPO default divergence + dead defaults in common.func | consistency | Medium | S | polish | - | in-progress |
| F2 | update_script exit conventions; error path exits 0 | consistency | Medium | S | polish | - | in-progress |
| F3 | camelCase locals in configure_devbox_server_profile | consistency | Low | S | polish | - | in-progress |
| F4 | Mixed ${VAR} vs $VAR styles | consistency | Low | S | polish | - | in-progress |
| F5 | `function` keyword only on ct entry points — document or normalize | consistency | Low | S | polish | - | in-progress |
| F6 | ct common.func fetch failure uses echo >&2 instead of available msg_error | consistency | Low | S | polish | - | in-progress |
| F7 | Whiptail cancel semantics: PAT/auto-update-time swallow Esc | consistency | Low | S | polish | - | in-progress |
| F8 | Redundant inline SC1091 disable | consistency | Low | S | polish | - | wontfix |
| G1 | Stale duplicate frontend/public/json/happier.json — delete or sync | docs | Medium | S | docs-truth | - | in-progress |
| G2 | Spec premise "Node already present on devbox" wrong — deviations note | docs | Medium | S | docs-truth | - | in-progress |
| G3 | Spec out-of-scope list contradicted by branch changes | docs | Low | S | docs-truth | - | in-progress |
| G4 | AUTO_UPDATE note unscoped — from_source silently ignores the knob | docs | Medium | S | docs-truth | - | in-progress |
| G5 | INSTALL_AGENTS note doesn't state default=1 (opt-out) | docs | Low | S | docs-truth | - | in-progress |
| G6 | Plan/backlog reference nonexistent .vscode/.shellcheckrc | docs | Low | S | docs-truth | - | in-progress |
| G7 | 2026-06-25 backlog #10 marked done but lint-only — re-status | docs | Medium | S | docs-truth | - | in-progress |
| G8 | Plan checkboxes unchecked though all 7 tasks committed | docs | Low | S | docs-truth | - | in-progress |
| G9 | INSTALLER_REPO/REF pinning knobs not user-documented | docs | Low | S | docs-truth | - | in-progress |

Status values: open · in-progress · pr-open · done · wontfix.
Update Issue # after GitHub sync; update Status as audit-remediate progresses.

Notes:
- Codex alignment: 34 agree / 3 modify / 5 needs-decision / 0 drop. A2, B1, B3, C3, C5 carry
  live-source evidence Codex could not verify from the repo alone (details in the report).
- 2 candidates dropped in adversarial verify (see report "Dropped" section).
- 2026-07-01 remediation: A1, A2, A3, A5, A6, A7, B1 fixed in the working tree (A4 and A8
  fixed as adjacent one-liners inside the A3 and A7/B1 blocks). Verified: bash -n + shellcheck
  clean, new code shfmt-clean, behavior smoke-tested. Pending: LXC matrix + commit → then done.
- 2026-07-01 remediation, docs epic: G1-G9 fixed in the working tree (frontend json synced,
  spec deviations section, plan status note + checkboxes ticked + rc path fixed, json notes
  scoped, 2026-06-25 backlog #10 re-statused superseded). Pending commit → then done.
- 2026-07-01 remediation, CI-gate epic: C1-C6 fixed in the working tree — checkout@v6;
  shellcheck v0.11.0 + shfmt v3.13.1 pinned with sha256 verification; happier-common.func added
  to full lint; excludes dropped entirely (shellcheck --norc -S info, per-line directives at the
  8 intentional sites); all three fork files reformatted shfmt-clean and the step's `|| true`
  removed; json step now asserts the installer contract fields. All four steps replayed green
  locally with the pinned tool versions. F8 → wontfix: with --norc in CI the inline SC1091
  directive is required, not redundant. Pending commit → then done.
- 2026-07-02 remediation, polish epic: F1-F7 fixed in the working tree — dead INSTALLER_REPO/REF
  defaults deleted from common.func (header comment corrected); update_script exits normalized
  (error path exit 1, success exit 0); camelCase locals renamed snake_case; quoted "$VAR" forms
  normalized to "${VAR}" across all three files (0 remnants; update-helper heredoc unaffected);
  `function` keyword documented as deliberate ct-template parity; ct common.func fetch errors now
  use msg_error; PAT/auto-update-time prompts state their Cancel behavior. Verified: bash -n,
  shellcheck 0.11 --norc -S info, shfmt 3.13.1 all clean. Pending commit → then done.
- 2026-07-02 remediation, unification epic: E1, E3-E8 fixed in the working tree.
  common.func gains find_happier_daemon_unit(), channel_default_stack_package(), and
  resolve_hstack_layout(); install script gains retry_until() (replacing 3 hand-rolled poll
  loops), shared enroll_tailscale_node() + enable_tailscale_serve_url() (managed path now gets
  the full invalid-key/timeout/AuthURL diagnostics and no longer enables Serve after a failed
  enrollment — the from_source-only helper definitions that made the drift structural were
  hoisted above the managed early-exit), and print_access_urls() /
  run_daemon_auth_if_requested() / finish_install() shared by both epilogues. ct gains
  fetch_remote_script() collapsing the two bootstrap fetch blocks (sourcing stays top-level —
  sourcing frameworks inside a function would make their declares function-local). E2 →
  wontfix: verify pass showed the URL chains are behaviorally consistent and their fallback
  tails legitimately differ per consumer; extraction would parameterize away the differences
  for no gain. Verified: bash -n + shellcheck --norc -S info + shfmt clean; helpers smoke-tested
  (incl. a live daemon-unit discovery hit on this host). Pending LXC + commit → then done.
- 2026-07-02 remediation, test-harness epic: D1-D3 fixed in the working tree — new fork-owned
  suite tests/happier/ (plain-bash asserts, no new deps): run.sh + lib.sh, channel-helper table
  tests (D2), URL/HH:MM validator edge cases incl. the python-heredoc silent-failure regression
  guard (D3; install script gained a testable is_valid_hhmm()), and sandboxed UI-bundle verify
  fixtures (D1: stub-minisign tier always, real-keypair tier in CI via HAPPIER_MINISIGN_PUBKEY
  override). Wired into happier-lint.yml (lint + dedicated test step installing minisign).
  ** The D1 happy-path test found a REAL latent bug (also on main): the UI archive was saved as
  happier-ui-web.tar.gz while the checksums file names the release asset, so `sha256sum -c`
  always failed and every managed UI-bundle install/refresh aborted at the checksum step.
  Fixed: archive now saved under its release-asset name (misc/happier-common.func). **
  D4 stays open: the LXC-matrix run record gets committed when the matrix is actually run.
- 2026-07-02 remediation, stragglers: A9 (tar/cp/mv failure paths in install_managed_ui_bundle
  now route through the cleanup helper, with best-effort old-version restore on a failed swap),
  B2 (tailscale pre-auth key passed via `--auth-key=file:<600 temp file>` instead of argv),
  B3 (claude installed via Anthropic's native installer as the happier user — vendor-recommended,
  self-updating, no root npm; codex stays vendor-documented npm; drop-in resolves
  ~happier/.local/bin/claude). install.sh URL verified live (HTTP 200, bash script). Spec
  deviations section updated. Full battery green after changes.
